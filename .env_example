# ============================================================================
# 🚀 BOT PACIFICA - CONFIGURAÇÃO COMPLETA
# ============================================================================
# Este arquivo contém TODAS as variáveis de ambiente utilizadas pelo bot.
# Copie este arquivo para .env e ajuste os valores conforme necessário.
# ============================================================================

# ============ CHAVES DE API E AUTENTICAÇÃO ============
MAIN_PUBLIC_KEY=
AGENT_PRIVATE_KEY_B58=
# Nota: Também aceita AGENT_PRIVATE_KEY (compatibilidade)

# ============ ENDPOINTS DA EXCHANGE ============
API_ADDRESS=https://api.pacifica.fi/api/v1
WS_BASE_URL=wss://ws.pacifica.fi/ws

# ============ CONFIGURAÇÃO DE LOGS E DEBUG ============
# Nível de log: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO
DEBUG_MODE=false

# ============ ESTRATÉGIA PRINCIPAL ============
# Tipos disponíveis:
# - "pure_grid": Grid clássico com range fixo
# - "market_making": Grid dinâmico que se adapta ao mercado
# - "dynamic_grid": Grid adaptativo inteligente com ajustes automáticos
# - "multi_asset": Scalping multi-asset básico
# - "multi_asset_enhanced": Algoritmo avançado com 5 indicadores técnicos
STRATEGY_TYPE=dynamic_grid

# ============ CONFIGURAÇÃO BÁSICA DE TRADING ============
# Símbolo principal para estratégias de grid (BTC, ETH, SOL, etc.)
SYMBOL=SOL

# Alavancagem para cálculo de margem
LEVERAGE=10

# Número máximo de ordens abertas simultaneamente
MAX_OPEN_ORDERS=20

# Verificar saldo antes de cada ordem?
CHECK_BALANCE_BEFORE_ORDER=true

# Intervalo entre rebalanceamentos (em segundos)
REBALANCE_INTERVAL_SECONDS=60

# Limpeza de ordens ao iniciar o bot
CLEAN_ORDERS_ON_START=false

# ============ CONFIGURAÇÕES DE GRID TRADING ============

# Número total de níveis do grid
GRID_LEVELS=8

# Tamanho de cada ordem em USD (valor notional)
ORDER_SIZE_USD=35

# Espaçamento entre níveis (em porcentagem)
# Ex: 0.2 = 0.2% entre cada nível
GRID_SPACING_PERCENT=0.2

# Distribuição do grid
# Opções: "symmetric" (50/50), "bullish" (60% buy), "bearish" (60% sell)
GRID_DISTRIBUTION=symmetric

# Modo do grid (maker, taker, etc.)
GRID_MODE=maker

# ============ RESET PERIÓDICO DO GRID ============
# Habilitar reset completo do grid periodicamente?
ENABLE_PERIODIC_GRID_RESET=true

# Intervalo para reset completo do grid (em minutos)
GRID_RESET_INTERVAL_MINUTES=60

# ============ GRID ADAPTATIVO ============
# Ajusta automaticamente o espaçamento baseado na volatilidade
ADAPTIVE_GRID=true
VOLATILITY_WINDOW=20
VOLATILITY_MULT_MIN=0.7
VOLATILITY_MULT_MAX=1.5

# ============ PURE GRID - CONFIGURAÇÕES ESPECÍFICAS ============
# (Apenas se STRATEGY_TYPE=pure_grid)

# Range de preços fixo para o grid
RANGE_MIN=90000
RANGE_MAX=110000

# Parar bot se preço sair do range?
RANGE_EXIT=true

# ============ MARKET MAKING GRID - CONFIGURAÇÕES ============
# (Apenas se STRATEGY_TYPE=market_making)

# Percentual de movimento para deslocar o grid
GRID_SHIFT_THRESHOLD_PERCENT=1.0

# ============ DYNAMIC GRID - CONFIGURAÇÕES AVANÇADAS ============
# (Apenas se STRATEGY_TYPE=dynamic_grid)

# Threshold para ajustes dinâmicos do grid
DYNAMIC_THRESHOLD_PERCENT=1.0

# Distância máxima de ajuste do grid
MAX_ADJUSTMENT_DISTANCE_PERCENT=5.0

# Habilitar boost de volume em condições específicas
VOLUME_BOOST_ENABLED=true

# ============ GERENCIAMENTO DE POSIÇÕES E MARGEM ============

# Tamanho máximo da posição em USD
MAX_POSITION_SIZE_USD=300

# Cancelamento automático de ordens em margem baixa
AUTO_CANCEL_ORDERS_ON_LOW_MARGIN=true
CANCEL_ORDERS_MARGIN_THRESHOLD=20        # Gatilho: margem < 20%
CANCEL_ORDERS_PERCENTAGE=30              # Cancelar 30% das ordens

# Redução automática de posição em margem crítica
AUTO_REDUCE_POSITION_ON_LOW_MARGIN=true
REDUCE_POSITION_MARGIN_THRESHOLD=10      # Gatilho: margem < 10%
REDUCE_POSITION_PERCENTAGE=20            # Vender 20% da posição

# Fechamento automático ao atingir limite de posição
AUTO_CLOSE_ON_MAX_POSITION=true

# Estratégia de fechamento automático:
# "cancel_distant_orders" = apenas cancela ordens distantes (conservador)
# "force_partial_sell" = sempre vende parte da posição (agressivo)
# "hybrid" = abordagem combinada (recomendado)
AUTO_CLOSE_STRATEGY=hybrid

# Percentual da posição a vender no fechamento automático
AUTO_CLOSE_PERCENTAGE=20

# ============ GESTÃO DE RISCO - CICLOS E SESSÕES ============

# Proteção por ciclo (nível 1)
ENABLE_CYCLE_PROTECTION=true
GRID_CYCLE_STOP_LOSS_PERCENT=5.0
GRID_CYCLE_TAKE_PROFIT_PERCENT=8.0

# Proteção de sessão (nível 2)
ENABLE_SESSION_PROTECTION=true
GRID_SESSION_MAX_LOSS_USD=80.0
GRID_SESSION_MAX_LOSS_PERCENT=20.0
GRID_SESSION_PROFIT_TARGET_USD=160.0
GRID_SESSION_PROFIT_TARGET_PERCENT=40.0

# Configurações de sessão alternativas
SESSION_STOP_LOSS_USD=100
SESSION_MAX_LOSS_USD=200
SESSION_PROFIT_TARGET_USD=500

# Ação ao atingir limites: "pause" ou "shutdown"
GRID_ACTION_ON_LIMIT=pause
GRID_PAUSE_DURATION_MINUTES=120

# Intervalos de verificação e logging
GRID_CHECK_INTERVAL_SECONDS=30
GRID_SAVE_PNL_HISTORY=true
GRID_LOG_PNL_EVERY_MINUTES=15

# Debug do Risk Manager
RISK_MANAGER_DEBUG_MODE=false

# ============ ESTRATÉGIAS MULTI-ASSET ============

# Símbolos para trading multi-asset
# AUTO = busca todos os símbolos disponíveis
# Ou especificar manualmente: BTC,ETH,SOL,AVAX (separados por vírgula)
SYMBOLS=AUTO

# Sistema de blacklist para filtrar símbolos indesejados
SYMBOLS_USE_BLACKLIST=true
SYMBOLS_BLACKLIST=PUMP,kPEPE,FARTCOIN
SYMBOLS_MAX_COUNT=0                      # 0 = sem limite

# Configurações básicas multi-asset
POSITION_SIZE_USD=20
MAX_CONCURRENT_TRADES=3
ALLOW_MULTIPLE_PER_SYMBOL=false
PRICE_CHANGE_THRESHOLD=0.3
PRICE_CHECK_INTERVAL_SECONDS=15

# ============ STOP LOSS E TAKE PROFIT ============

# TP/SL automáticos (Camada 2 de proteção)
AUTO_CLOSE_ENABLED=true
USE_API_TP_SL=true                       # true = via API, false = monitoramento manual
STOP_LOSS_PERCENT=1.0
TAKE_PROFIT_PERCENT=1.5

# Trailing Stop (experimental)
TRAILING_STOP_ENABLED=false
TRAILING_STOP_PERCENT=0.5

# Tempo máximo de posição (forçar fechamento)
MAX_POSITION_TIME_MINUTES=60

# ============ SISTEMA DE EMERGÊNCIA (CAMADA 3) ============
# Proteção de última instância quando outras falham

EMERGENCY_SL_PERCENT=3.0                 # Perda crítica
EMERGENCY_TP_PERCENT=5.0                 # Lucro extremo para proteção
EMERGENCY_MAX_LOSS_TIME_MINUTES=15       # Tempo máximo em perda
EMERGENCY_CHECK_INTERVAL_SECONDS=10      # Frequência de verificação

# ============ ESTRATÉGIA ENHANCED - INDICADORES TÉCNICOS ============
# (Apenas se STRATEGY_TYPE=multi_asset_enhanced)

# Configurações de qualidade do sinal
ENHANCED_MIN_SIGNAL_QUALITY=65           # Qualidade mínima (0-100)
ENHANCED_MIN_CONFIDENCE=75               # Confiança mínima (0-100)
ENHANCED_USE_RSI_FILTER=true             # Filtrar por RSI extremos
ENHANCED_MAX_VOLATILITY=4.0              # Volatilidade máxima permitida (%)
ENHANCED_MIN_HISTORY=25                  # Períodos mínimos para análise

# ============ NOTIFICAÇÕES TELEGRAM ============
# Sistema de notificações resiliente

TELEGRAM_ENABLED=false
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Configurações avançadas do Telegram
TELEGRAM_TIMEOUT_SECONDS=45
TELEGRAM_CONNECT_TIMEOUT=20
TELEGRAM_MAX_RETRIES=5
TELEGRAM_RETRY_DELAY_SECONDS=3.0
TELEGRAM_RATE_LIMIT_SECONDS=2.0

# ============================================================================
# 📋 RESUMO DAS PRINCIPAIS CONFIGURAÇÕES
# ============================================================================
# 
# INICIANTE (Configuração Conservadora):
# - STRATEGY_TYPE=market_making
# - GRID_LEVELS=6
# - ORDER_SIZE_USD=25
# - GRID_SPACING_PERCENT=0.3
# - LEVERAGE=5
# 
# INTERMEDIÁRIO (Configuração Equilibrada):
# - STRATEGY_TYPE=dynamic_grid  
# - GRID_LEVELS=8
# - ORDER_SIZE_USD=35
# - GRID_SPACING_PERCENT=0.2
# - LEVERAGE=10
# 
# AVANÇADO (Configuração Agressiva):
# - STRATEGY_TYPE=multi_asset_enhanced
# - POSITION_SIZE_USD=50
# - MAX_CONCURRENT_TRADES=5
# - LEVERAGE=15
# 
# ============================================================================